# Setup
1. copy `sample.config.json` to `config.json`
2. create a discord app and add the token and app id to `config.json`
3. create a pushover app and add the token, user key, and a list of devices to `config.json`
4. run `npm run start`